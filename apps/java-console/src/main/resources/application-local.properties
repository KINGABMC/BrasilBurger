# ============================================
# CONFIGURATION DÉVELOPPEMENT LOCAL - BRASIL BURGER
# ============================================

# Database Configuration (PostgreSQL LOCAL)
db.url=jdbc:postgresql://localhost:5432/brasilburger_local
db.username=postgres
db.password=Postgres@2025
db.pool.size=5
db.driver=org.postgresql.Driver

# Cloudinary Configuration (pour tests locaux)
cloudinary.cloud_name=dev
cloudinary.api_key=dev_key
cloudinary.api_secret=dev_secret
cloudinary.secure=true

# Application Configuration
app.name=Brasil Burger Java Console
app.version=1.0.0
app.environment=development
app.debug=true

# Logging Configuration
logging.level.com.brasilburger=INFO
logging.level.root=WARN

# Console Configuration
console.encoding=UTF-8
console.width=80